{
  "buildCommand": "npm run build",
  "outputDirectory": ".vercel/output",
  "framework": "vite",
  "functions": {
    "api/index.js": {
      "runtime": "nodejs20.x",
      "handler": "vercel-server.js"
    }
  },
  "routes": [
    { "src": "/(.*)", "dest": "/api/index.js" }
  ]
}
