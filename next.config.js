// @ts-nocheck
// @ts-ignore
 const { withBlitz } = require("@blitzjs/next")

/** @type {import('next').NextConfig} */
const nextConfig = {
    experimental: {
    typedRoutes: true,
  },

  /* config options here */
}
 
module.exports = nextConfig